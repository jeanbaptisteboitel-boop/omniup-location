{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx prisma migrate deploy && npm run build",
  "crons": [
    {
      "path": "/api/cron/loyers",
      "schedule": "0 6 * * *"
    }
  ]
}
