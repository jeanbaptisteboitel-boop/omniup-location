# ---- Base de données PostgreSQL (Neon en production)
# DATABASE_URL : chaîne « pooled » (hôte -pooler) utilisée par l'application
# DIRECT_URL   : chaîne directe utilisée par les migrations (prisma migrate deploy)
DATABASE_URL="postgresql://USER:MOT_DE_PASSE@ep-xxxx-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://USER:MOT_DE_PASSE@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require"
# En local : postgresql://omniup:omniup@localhost:5432/omniup?schema=public (même valeur pour les deux)

# ---- Fichiers importés (pièces d'identité, justificatifs, factures)
# Stockage objet Scaleway (obligatoire sur Vercel). Sans ces variables, les fichiers sont écrits sur le disque local.
SCW_ACCESS_KEY=
SCW_SECRET_KEY=
SCW_BUCKET=
SCW_REGION=fr-par
# SCW_ENDPOINT=https://s3.fr-par.scw.cloud   (déduit de la région si absent)
# Dossier local utilisé quand le stockage objet n'est pas configuré
STORAGE_DIR="./storage"

# ---- Adresse publique de l'application (ex. https://votre-app.vercel.app) : origine autorisée à envoyer les fichiers
# directement vers le bucket (bouton « Autoriser l'envoi direct » dans Paramètres ou scripts/configurer-cors.mjs)
APP_URL=

# ---- Protection de l'application (obligatoire dès qu'elle est accessible sur Internet)
# APP_SECRET : secret aléatoire (32 caractères ou plus, ex. `openssl rand -hex 32`) qui signe le cookie de session et active
#              les comptes utilisateurs : à la première ouverture de /connexion, on crée le compte super-administrateur.
# APP_PASSWORD (facultatif) : mot de passe principal, demandé en plus pour créer le premier compte et utilisable en secours
#              (/connexion?mode=principal). Seul, il protège l'application comme avant (sans comptes).
APP_PASSWORD=
APP_SECRET=

# ---- Protection anti-robots (Cloudflare Turnstile) sur les formulaires de connexion, de mot de passe oublié et de lien
# d'accès perdu (espaces locataire et propriétaire) — facultatif. Créez un widget sur https://dash.cloudflare.com
# (Turnstile) avec le domaine de l'application, puis renseignez ses deux clés.
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ---- Envoi d'emails — facultatif ; sans configuration, les envois sont désactivés (les PDF restent téléchargeables)
# Fournisseur recommandé : Resend (https://resend.com). Créez une clé API et vérifiez votre domaine d'envoi.
RESEND_API_KEY=
# Adresse d'expédition (domaine vérifié chez Resend) ; sert aussi au SMTP
MAIL_FROM="Gestion locative <gestion@exemple.fr>"
# Alternative : serveur SMTP classique, utilisé seulement si RESEND_API_KEY est vide
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# ---- Appels de loyer
# Nombre de jours avant le début du mois pour émettre les avis d'échéance
AVIS_JOURS_AVANCE=10
# Envoi automatique par email des avis nouvellement émis lors de l'exécution planifiée (/api/cron/loyers)
AVIS_ENVOI_AUTO=false
# Secret protégeant /api/cron/loyers (Vercel l'envoie automatiquement en en-tête Authorization: Bearer)
CRON_SECRET=

# ---- Assistant de rédaction (Anthropic) — contrats, courriers, emails
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5

# ---- OCR et extraction (Mistral) — lecture des échéanciers PDF/images et des justificatifs
MISTRAL_API_KEY=
MISTRAL_MODEL_OCR=mistral-ocr-latest
MISTRAL_MODEL_EXTRACTION=mistral-medium-latest

# ---- Indices INSEE (IRL, ILC, ILAT, ICC, BT01…) : service de données public, sans clé, synchronisé chaque jour (/api/cron/indices, CRON_SECRET)
# Base du service, à changer seulement pour passer par un relais
# INSEE_URL=https://bdm.insee.fr
# Adresse recevant les alertes (échecs répétés, série arrêtée ou rebasée, libellé modifié) quand l'envoi d'emails est configuré
ALERTES_EMAIL=

# ---- Assistance : adresse recevant les tickets ouverts depuis le bouton « Aide » (aide, problème, proposition)
# Sans cette variable, les tickets restent consultables dans l'application (rubrique Assistance) sans être envoyés.
SUPPORT_EMAIL=

# ---- Omniup Sign (signature électronique) — intégration à venir
OMNIUP_SIGN_URL=
OMNIUP_SIGN_API_KEY=
